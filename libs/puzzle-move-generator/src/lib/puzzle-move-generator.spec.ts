import { puzzleMoveGenerator } from './puzzle-move-generator';

describe('puzzleMoveGenerator', () => {
  it('should work', () => {
    expect(puzzleMoveGenerator()).toEqual('puzzle-move-generator');
  });
});
