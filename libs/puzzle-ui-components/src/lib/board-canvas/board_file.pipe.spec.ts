import { BoardFilePipe } from './board_file.pipe';

describe('BoardFilePipe', () => {
  it('create an instance', () => {
    const pipe = new BoardFilePipe();
    expect(pipe).toBeTruthy();
  });
});
