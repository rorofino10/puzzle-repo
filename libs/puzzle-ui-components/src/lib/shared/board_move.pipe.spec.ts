import { BoardMovePipe } from './board_move.pipe';

describe('BoardMovePipe', () => {
  it('create an instance', () => {
    const pipe = new BoardMovePipe();
    expect(pipe).toBeTruthy();
  });
});
